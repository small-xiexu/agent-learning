spring:
  config:
    import: optional:application-openai-graph-demo-local.yml

llm:
  provider: openai-compatible
  base-url: https://api.openai.com
  api-key:
  model: gpt-4o
  chat-completions-path: /v1/chat/completions

demo:
  graph:
    openai:
      enabled: false
