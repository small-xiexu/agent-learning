spring:
  config:
    import: optional:application-openai-camel-demo-local.yml

llm:
  provider: openai-compatible
  base-url: https://api.openai.com
  api-key:
  model: gpt-4o

demo:
  camel:
    openai:
      enabled: false
