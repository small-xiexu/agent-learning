spring:
  config:
    import: optional:nested/import-chain.yml

llm:
  provider: openai-compatible
  base-url: https://api.openai.com
  api-key:
  model: gpt-4o

demo:
  sample:
    openai:
      enabled: false
