spring:
  config:
    import: optional:application-openai-supervisor-demo-local.yml

llm:
  provider: openai-compatible
  base-url: https://api.openai.com
  api-key: your-openai-api-key
  model: gpt-4o

demo:
  supervisor:
    openai:
      enabled: false
