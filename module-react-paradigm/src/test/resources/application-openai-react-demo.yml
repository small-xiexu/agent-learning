spring:
  config:
    import:
      - optional:application-llm-local.yml
      - optional:application-openai-react-demo-local.yml

llm:
  provider: openai-compatible
  base-url: https://api.openai.com
  api-key:
  model: gpt-4o
  chat-completions-path: /v1/chat/completions

demo:
  react:
    openai:
      enabled: false
