spring:
  config:
    import: optional:application-openai-roleplay-demo-local.yml

llm:
  provider: openai-compatible
  base-url: https://api.openai.com
  api-key:
  model: gpt-4o
  chat-completions-path: /v1/chat/completions

demo:
  roleplay:
    openai:
      enabled: false
